package com.youpin.comic.green.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import com.youpin.comic.mainpage.bean.SearchKeyWord;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SEARCH_KEY_WORD".
*/
public class SearchKeyWordDao extends AbstractDao<SearchKeyWord, Long> {

    public static final String TABLENAME = "SEARCH_KEY_WORD";

    /**
     * Properties of entity SearchKeyWord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property UserId = new Property(0, Long.class, "userId", true, "_id");
        public final static Property Type = new Property(1, int.class, "type", false, "TYPE");
        public final static Property Keyword = new Property(2, String.class, "keyword", false, "KEYWORD");
        public final static Property SearchTime = new Property(3, String.class, "searchTime", false, "SEARCH_TIME");
    }


    public SearchKeyWordDao(DaoConfig config) {
        super(config);
    }
    
    public SearchKeyWordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SEARCH_KEY_WORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: userId
                "\"TYPE\" INTEGER NOT NULL ," + // 1: type
                "\"KEYWORD\" TEXT," + // 2: keyword
                "\"SEARCH_TIME\" TEXT);"); // 3: searchTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SEARCH_KEY_WORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SearchKeyWord entity) {
        stmt.clearBindings();
 
        Long userId = entity.getUserId();
        if (userId != null) {
            stmt.bindLong(1, userId);
        }
        stmt.bindLong(2, entity.getType());
 
        String keyword = entity.getKeyword();
        if (keyword != null) {
            stmt.bindString(3, keyword);
        }
 
        String searchTime = entity.getSearchTime();
        if (searchTime != null) {
            stmt.bindString(4, searchTime);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SearchKeyWord entity) {
        stmt.clearBindings();
 
        Long userId = entity.getUserId();
        if (userId != null) {
            stmt.bindLong(1, userId);
        }
        stmt.bindLong(2, entity.getType());
 
        String keyword = entity.getKeyword();
        if (keyword != null) {
            stmt.bindString(3, keyword);
        }
 
        String searchTime = entity.getSearchTime();
        if (searchTime != null) {
            stmt.bindString(4, searchTime);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SearchKeyWord readEntity(Cursor cursor, int offset) {
        SearchKeyWord entity = new SearchKeyWord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // userId
            cursor.getInt(offset + 1), // type
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // keyword
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // searchTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SearchKeyWord entity, int offset) {
        entity.setUserId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setType(cursor.getInt(offset + 1));
        entity.setKeyword(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSearchTime(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SearchKeyWord entity, long rowId) {
        entity.setUserId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SearchKeyWord entity) {
        if(entity != null) {
            return entity.getUserId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SearchKeyWord entity) {
        return entity.getUserId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
